// Edit this to point at your deployed Solarch backend (Railway/Render/etc).
// For local testing, leave as-is (assumes `solarch serve --port 8090` running locally).
window.__NEXUSDOCS_API_BASE__ = "https://nexusdocs.onrender.com";

// Once your backend is live on Railway, change it to something like:
// window.__NEXUSDOCS_API_BASE__ = "https://nexusdocs-production.up.railway.app";